replicaCount: 1

image:
  # customise this to your own repository
  repository: bennorris12345/civo-llm-boilerplate
  pullPolicy: Always
  tag: "latest"

service:
  type: LoadBalancer
  port: 80

model:
  textModel: "llama3.2:latest"
  imageModel: "x/llama3.2-vision:latest"